console.log("Nothing is worth more than laughter.");

let bestSong = Math.random() > 0.5
    ? 'Chain of fools'
    : 'secrets';


let sample = '';

let array: Array<string> = ['asda', 'asdas'];

array.forEach(element => {
    console.log(element);

});

let user: {
        readonly firstName: string
    } = {
        firstName: 'abby'
    }

user.firstName = 'sample';
