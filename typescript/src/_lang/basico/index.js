console.log("Nothing is worth more than laughter.");
var bestSong = Math.random() > 0.5
    ? 'Chain of fools'
    : 'secrets';
var sample = '';
var array = ['asda', 'asdas'];
array.forEach(function (element) {
    console.log(element);
});
